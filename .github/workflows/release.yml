name: release

on:
  push:
    branches:
      - master

jobs:
  build:
    name: Building app
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [10]

    steps:
      - uses: actions/checkout@v1
      - name: building...
        run: |
          yarn
          yarn build
